!!!
html
  head
    title= title
    link(rel='stylesheet', href='stylesheets/style.css')
    //block include_scripts
    //allign not working on image, not sure if layout format is correct
  body
    #header
      img(src='/images/subwaylabs.jpg', height='100px', align='center')
      - if (session.user)
        #user= 'Logged in as ' + session.user.login
        #logout
          a(href='/sessions/destroy') logout
    - if (flash.warn)
      p#warn=flash.warn
    #container!=body
